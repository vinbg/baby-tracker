# PostgreSQL connection string
DATABASE_URL=postgres://USER@localhost:5432/baby_feeding

# API server
PORT=3101

# Baby profile shown in the app
BABY_NAME=Baby
BABY_BIRTH_DATE=2026-01-30

# Login/session settings
AUTH_USER=veli
AUTH_TOKEN=replace-with-a-long-random-token
# SESSION_SECRET can be used as a fallback for AUTH_TOKEN if needed.
# SESSION_SECRET=replace-with-a-long-random-secret
